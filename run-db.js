require('dotenv').config(); const { PrismaClient } = require('@prisma/client'); const prisma = new PrismaClient(); async function main() { const user = await prisma.user.findUnique({ where: { email: 'optika.narodnaya.astana@gmail.com' } }); if (!user) return; const org = await prisma.organization.update({ where: { id: user.organizationId }, data: { type: 'headquarters' } }); console.log('Fixed org:', org.name); } main();
