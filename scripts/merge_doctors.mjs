// Simple script to merge doctors via the app's own API
// Run with: node scripts/merge_doctors.mjs

const BASE = 'https://lensflow-crm.vercel.app';

async function main() {
  // We'll use the prisma client directly through a one-off API endpoint
  // Instead, let's create a temporary API route
  console.log('Creating temporary merge endpoint...');
}

main();
