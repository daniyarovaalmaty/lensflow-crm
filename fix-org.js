const { PrismaClient } = require('@prisma/client'); const prisma = new PrismaClient(); async function main() { const user = await prisma.user.findUnique({ where: { email: 'optika.narodnaya.astana@gmail.com' } }); if (!user) { console.log('User not found'); return; } const org = await prisma.organization.update({ where: { id: user.organizationId }, data: { type: 'headquarters' } }); console.log('Successfully updated org:', org.name, 'to type:', org.type); } main().finally(() => prisma['$disconnect']());
